{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run App Book Stack",
            "request": "launch",
            "type": "dart",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/lib/main.dart",
        }
    ]
}